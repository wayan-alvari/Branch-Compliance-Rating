@{ ViewData["Title"] = "Workspace overview"; }
<section class="card"><div class="card-body">
    <h2 class="h4">Your assessment workspace</h2>
    <p>Signed in as <strong>@User.Identity?.Name</strong>.</p>
    <p class="mb-0">A transparent assessment connects branch evidence, criterion scores, appeal decisions, and a final rating. Use the same browser when switching roles.</p>
</div></section>
